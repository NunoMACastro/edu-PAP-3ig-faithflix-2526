# Cliente API FaithFlix

Esta pasta contem o cliente API central criado no `BK-MF1-03`.

Todas as chamadas HTTP do frontend devem passar por `apiClient`. Nao colocar `fetch` direto nas paginas, para manter cookies, tratamento de erro e mensagens consistentes.
