import { AppRoutes } from "./routes/AppRoutes.jsx";

/**
 * Root React component for the FaithFlix frontend.
 *
 * @returns {JSX.Element} Application routes wrapped by the configured router.
 */
export function App() {
    return <AppRoutes />;
}
