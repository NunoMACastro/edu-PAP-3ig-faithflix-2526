/**
 * @file Ficheiro `real_dev/frontend/src/App.jsx` da implementação real_dev.
 */

import { AppRoutes } from "./routes/AppRoutes.jsx";

/**
 * Componente React raiz do frontend FaithFlix.
 *
 * @returns {JSX.Element} Rotas da aplicação envolvidas pelo router configurado.
 */
export function App() {
    return <AppRoutes />;
}
