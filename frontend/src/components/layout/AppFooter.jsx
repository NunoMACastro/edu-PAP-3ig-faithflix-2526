/**
 * @file Ficheiro `real_dev/frontend/src/components/layout/AppFooter.jsx` da implementação real_dev.
 */

/**
 * Rodapé partilhado com identificação exclusivamente editorial da marca.
 *
 * @returns {JSX.Element} Rodapé público da aplicação.
 */
export function AppFooter() {
    return (
        <footer className="app-footer">
            <div>
                <strong>FaithFlix</strong>
            </div>
            <span>Conteúdo, comunidade e impacto solidário.</span>
        </footer>
    );
}
